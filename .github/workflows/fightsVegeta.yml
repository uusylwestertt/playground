name: Fights Vegeta
run-name: Fights Vegeta

on:
  schedule:
     - cron: "*/20 1-23 * * *"
  workflow_dispatch:

jobs:
  FightsVegeta:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: "Fights vegeta"
        run: |
          touch .env
          echo "LOGIN=${{ secrets.LOGIN_VEGETA }}" >> .env
          echo "PASSWORD=${{ secrets.PASSWORD_LYYSYY_VEGETA }}" >> .env
          npx playwright install firefox
          npm run safariTest
    